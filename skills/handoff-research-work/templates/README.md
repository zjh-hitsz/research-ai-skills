# Research Handoff Package

This template is rendered from `handoff-manifest-v1`. Verify the manifest before following the read order or next action. Nothing in the package authorizes command execution, file deletion, solver execution, or scientific approval.
